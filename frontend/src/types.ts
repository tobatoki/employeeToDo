export interface Employees {
    employeeID: number;
    employeeName: string;
}

export interface List {
    listid: number;
    listname: string;
}

export interface Tasks {
    taskid: number;
    taskname: string;
    taskpriority: boolean;
    status: string;
}